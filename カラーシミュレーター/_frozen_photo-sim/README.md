# 凍結フォルダ（作品写真ベースのカラーシミュレーション）

2026-07-06、作品写真をアップロード/選択して色を変える方式のカラーシミュレーションは断念し、
`カラーシミュレーター/pattern-color-proto/`（型紙SVG×革テクスチャ方式）に専念する方針となった。

このフォルダの中身（`photo-color.html` / `image-proxy.gs` / `IMAGE_PROXY_SETUP.md`）は
その断念した方式の実装。サイト内の参照リンクはすべて削除済み（index.html / works.html /
カラーシミュレーター/simulator.html）。**新規リンクを張らないこと。**

なお `カラーシミュレーター/simulator.html` と `カラーシミュレーター/assets/models/` は、
`order_estimate/leather-order-estimate-v2.html`（フルオーダー見積もりページ）のカラー
シミュレーター連携で現役使用中のため、このフォルダには含めていない。
