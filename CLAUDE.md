# Claude Code 作業記録

このファイルは、Claude Code がリポジトリを開いたときに変更意図を把握できるように残す作業メモです。

**構成:** ①恒久ルール（毎回読む） → ②直近の作業記録 → ③領域別ノートの索引 → ④過去分のアーカイブ索引。
**追記するとき:** 新しい記録は「直近の作業記録」の**先頭**に足す（日付降順）。監理幕僚室ツール・ダイエットPWAの記録は `docs/notes/` の該当ファイルへ。恒久的な禁止事項を作ったら①にも1行足す。
**月が変わったら:** `node tools/archive_claudemd.js` でドライラン → 内容を確認して `--apply` を付けて実行する。古い月が `docs/worklog/YYYY-MM.md` へ移り、索引の件数まで自動更新される。

---

## ⛔ 恒久ルール（主題別・毎回読むのはここだけ）

> 各行の末尾は出典。日付だけのものは本ファイル「直近の作業記録」に、
> 「アーカイブ」付きは `docs/worklog/` に全文（経緯・実測値・検証手順）がある。
> **禁止の理由まで知りたいときだけ出典を開くこと。**

### 公開範囲 — このリポジトリは public。置いた時点で全世界に読まれる
- ⛔ `receipt-checklist.json` に証憑の**本文**を入れない（氏名・自宅住所・メール・注文番号が入る）— 2026-08-22
- ⛔ 同 `health` に**認証情報**を入れない。出すのは日付と件数だけ — 2026-08-22
- ⛔ `camera` 列に店名・ファイルパス・メモを出さない。明細は管理者キー保護側で見る — 2026-08-23
- ⛔ 経理ルール（店名・家事按分率）を公開ファイルに戻さない。正本は `監理部\経理ルール.md` — 2026-08-23
- ⛔ 配布用ZIPを作り直したら必ず `保全部\check_public_leak.py` を通す（初回ビルドで氏名・BTC保有量・不動産決済額が混入した）— 2026-08-21
- ⛔ 第三者の投稿を public な Issue に記録しない — 2026-08-25・アーカイブ
- ⛔ 公開ページのボタンに API トークンを持ち込まない。自動投稿化するならサーバー側にトークンを持つ GAS 経由 — 2026-09-01
- ⛔ JSゲート（合言葉・SHA-256）は**UIの出し分けであって機密保護ではない**。非公開データを扱わせない — 2026-09-01

### GAS通信・冪等性 — 単純リトライは重複を生む
- ⛔ 書き込み系APIを足すときは**先に冪等化してから**リトライを付ける（`requestId` 方式）— 2026-08-04・アーカイブ
- ⛔ `loadJsonp` / `jsonpCallRetry` のリトライを書き込み系に流用しない（実投稿の二重化・Gemini二重課金）— 2026-08-06 / 08-12・アーカイブ
- ⛔ GAS呼び出しは `gas()` ラッパーを通す（HTML応答・間欠404を3回まで再送）— 2026-08-23
- ⛔ **HTTP 200 でもHTMLが返る。** ステータスコードでなく本文先頭が `<` かで判定する — 2026-08-04・アーカイブ
- ⛔ GASは同じPOSTでも間欠的に404＋HTMLを返す。再送しない呼び出しを新設しない — 2026-09-04
- ⛔ その再送は「GASに届かなかった応答」（HTTP非200・本文先頭が `<`）だけに限る。タイムアウト・通信断で再送するとAI呼び出しが二重課金 — 2026-09-04
- ⛔ 通信失敗の文言に原因を断定して書かない（「管理者のGAS承認待ち」が権限問題の誤診断を招いた）— 2026-09-04
- ⛔ 掲載成功後のThreads告知は必ず `try/catch` で囲む（告知の失敗でお知らせが二重に載る）— 2026-08-06・アーカイブ

### 会計・二重計上 — 一度誤ると計上漏れは永久に戻らない
- ⛔ ラクマは件名「取引完了」の1通だけを拾う。広げると同じ取引が二重計上になる — 2026-08-23
- ⛔ `camera` 列を会計送信の対象に戻さない（レシート取込側がCSVを出すため二重になる）— 2026-08-23
- ⛔ 店名を見ない照合を復活させない（無関係な同額取引と一致し、その経費は二度とCSVに出ない）— 2026-08-24
- ⛔ `storeMatch()` の `'unknown'` を `'yes'` に丸めない。「要確認」として人の判断に回す — 2026-08-24
- ⛔ 総平均法は「通貨ごと」に全取引所を合算する。取引所別に作ると所得金額が変わる — 2026-08-21
- ⛔ JavaScript の `Number` で金額計算をしない。**BigInt の固定小数点（小数20桁）**を使う — 2026-08-21

### 作品集・OGP・お知らせ欄
- ⛔ `.work-card.is-unavailable` に `opacity` / `filter: grayscale` を復活させない（革の色味で見せる場のため）— 2026-08-04・アーカイブ
- ⛔ 作品ページに `meta http-equiv="refresh"` を復活させない。転送は `location.replace` のみ（Metaのクローラが追ってしまう）— 2026-08-12・アーカイブ
- ⛔ `og:image` に `drive.google.com/thumbnail` を使わない。`lh3.googleusercontent.com/d/<fileId>=w1200` を組み立てる — 2026-08-12・アーカイブ
- ⛔ `openDeepLinkedWork` で Drive の写真取得を待ってからモーダルを開かない（GAS不調で永久に開かない）— 2026-08-12・アーカイブ
- ⛔ お知らせ欄の描画を「置き換え」に戻さない。GAS が書いた `<li>` が消える — 2026-08-06・アーカイブ
- ⛔ Gmailラベル `お知らせ処理済み` を変えるときは `NEWS_REPLY_LABEL` も揃える — 2026-08-06・アーカイブ
- ⛔ `STATIC_WORKS_URL` の相対パス `../../works-data.json` はページ階層依存。ツールを移したら追従させる — 2026-08-12・アーカイブ
- ⛔ `works/` 配下は全て自動生成。手で編集せず `tools/build_work_pages.js` を直す — 2026-08-12・アーカイブ

### 画面の「生きている／止まっている」表示
- ⛔ ticker 稼働中に `setLog` を呼ばない（毎秒の書き換えで即上書きされる）。文言変更は `updateProgressTicker` で — 2026-08-20
- ⛔ `setDriveProgress` から `setLog` / `els.log` を触らない（スマホのトーストが出っぱなしになる）— 2026-08-22
- ⛔ **フォールバックを出すときは必ず復旧導線もセットで置く。** 「安全に止まる」だけでは実質使用不能 — 2026-08-23
- ⛔ 初回セットアップ画面の「バックアップから復元」を消さない — 2026-08-24
- ⛔ 未実装の仮画面を出して `return` しない（入力手段そのものが無くなる）— 2026-08-24
- ⛔ `renderRoute` の世代トークン（`state.renderToken`）ガードを外さない（描画の競合が戻る）— 2026-08-24
- ⛔ 「表が埋まっている＝取得できている」ではない。取得の生死は画面に別途出す（8日間気付かなかった）— 2026-08-22

### 請求書・書類番号
- ⛔ `editor.js` から `API.getNextNumber` を呼び戻さない。画面を開くたびに番号が飛ぶ — 2026-08-24
- ⛔ 新規保存が通ったら `editId` を必ず立てる。立て忘れると別番号の書類が増える — 2026-08-24

### PWA・Service Worker
- ⛔ `assets/` や `index.html` を変更したら `sw.js` の `CACHE` を必ずインクリメントする — 2026-08-24
- ⛔ iPhone は Safari とホーム画面アイコンで**保存領域が別扱い**になる。「先にホーム画面へ追加」の案内を消さない — 2026-08-24
- ⛔ `/diet/` は外部ライブラリ・CDN禁止（`connect-src 'none'`）。noindex運用で sitemap に載せない — 2026-08-24
- ⛔ 運動の消費kcalは `(METs - 1) × 体重kg × 時間h × 1.05`。`METs ×` のままだと安静時代謝の二重計上 — 2026-08-24

### レシート取込
- ⛔ 解析モデルはサーバー側固定（`claude-haiku-4-5`）。変更は ScriptProperties `RECEIPT_MODEL` — 2026-08-23
- ⛔ `rules.json` だけを直さない。正本は `監理部\経理ルール.md` で両方同時に更新する — 2026-08-23
- ⛔ 科目ボタンと `select` の値がずれないよう、更新は必ず `setAccount()` を通す — 2026-08-23

### 動画メーカー
- ⛔ フィルムシーン中は写真ごとのカメラワーク・切替効果が**視覚上無効**（仕様。時間割だけ使う）— 2026-08-25
- ⛔ CTA文面に売り込みコピー（今すぐ購入等）を入れない（SNS発信方針 第6章）— 2026-08-26

### 撤収済み案件 — 再開するなら先にここを読む
- ⛔ Threads言及監視: 投稿用トークンで `/keyword_search` を叩くと 401/403 ではなく **HTTP 500 / code=1** が返る（＝検索スコープが無いだけ）— 2026-08-25・アーカイブ
- ⛔ Instagram で同じことはできない（自由文キーワード検索APIが存在しない）— 2026-08-25・アーカイブ

### トップページのナビ
- ⛔ `.nav-links` / `.nav` に `overflow`（`overflow-x:auto` 含む）を付けない。CSS仕様で縦方向も同時にクリップされ、幕僚室ドロップダウンがナビ枠内で切り取られて「押しても何も出ない」になる（9/1〜9/4の3日間気付かなかった）。狭い幅は余白を詰めて収める — 2026-09-04
- ⛔ インラインJSの i18n 辞書に `N's` / `Commander's` などアポストロフィを含む値を単引用符で書かない。同じ `<script>` ブロックの全機能が止まる。push前に `node --check` — 2026-09-03

### 検証の作法（過去に誤判定した罠）
- GitHub Pages は `Cache-Control: max-age=600`。デプロイ直後10分は**サーバは新・ブラウザは旧**。`?cb=` か配信HTMLのgrepで「今見ているのが新コードか」を確かめてから判定する — 2026-08-12・アーカイブ
- 本番 https ページから `http://127.0.0.1` への **fetch はブラウザが送出前に遮断する**（`window.open` は通る）。この経路の検証は必ず本番URLで行う — 2026-08-22

---

## 直近の作業記録（2026-08-20 以降）

## 2026-09-03 — デジタル幕僚室: ライブテロップ／テロッププレーヤーを追加

- 対象: `digital-room/live-telop.html`（配信用リアルタイム字幕・Chrome/Edgeの音声認識・緑背景でOBSクロマキー）、`digital-room/telop-player.html`（動画＋SRTをブラウザで同期再生・焼き込み不要）、`digital-room/index.html`「発信・制作」にカード2枚（`data-app="live-telop"` / `"telop-player"`）。
- 告知画像: `assets/live-telop-announce.png` / `telop-player-announce.png`。生成は `広報部\告知画像\promo_template.py`（apps.json に2件追加）。1枚ずつ手作りしない。
- 「文字起こし＆テロップ」との違い: あちらは**録画済み動画**の文字起こし・焼き込み（ダウンロード配布のPCアプリ）。ライブテロップは**生放送中**の音声をその場で字幕表示、テロッププレーヤーは**できたSRTを重ねて再生**する再生機。見出し文言はこの違いが出るように書く。
- 検証: 本番URL2本 HTTP 200・md5一致、Chrome以外での警告表示（SpeechRecognition無効化）実測、カード4枚の幅335px一致（Playwright 1280px）。
- 追記（同日）: 3つの区別が付きにくいとの指摘で、カード名に用途を付与（撮った後／配信中／上映用）し、3枚の直前に `grid-column:1/-1` の使い分け行を追加。全部を1本に統合しなかった理由は、文字起こし＆テロップがWhisperを使うPCアプリでブラウザに載らないため。
- 配信先別ガイド `digital-room/live-telop-guide.html` を新設（インスタライブ＝PC版ライブプロデューサー＋OBS・プロアカウント必須／YouTube Live／Zoom仮想カメラ／展示用途）。カードと設定パネルからリンク。**sitemap.xml 未登録・サイト構成MAPは作戦幕僚へ更新依頼済み**（commit 6c5e8a1）。

## 2026-09-01 — SNS動画メーカー: 「SNSへ投稿」ボタン（管理者限定・共有シート方式）

- 対象: `digital-room/sns-video-maker/index.html`
- 要望: 動画生成後、作品集掲載のキャプションを付けてそのままSNSへ投稿するボタン。**一般利用者には見せない**（公開アプリのため）。
- 方式: **Web Share API（OSの共有シート）**。`navigator.share({files:[mp4], text:キャプション})` でスマホの共有シートを開き、事業主がInstagram等を選んで投稿する。APIトークン不使用・自動公開なし。キャプションは保険で先にクリップボードへコピー（共有先が本文を受け取らないアプリ対策）。PC等の非対応環境はキャプションコピー＋ダウンロード案内にフォールバック。
- 管理者限定の実装:
  - `NSF_GATE_HASH`（works.html と同一の公開済みSHA-256定数）＋ `sessionStorage['nsf_admin_key']` 共有。works.html 側で管理者モードONにすれば動画メーカーでも有効。
  - **works.html と違い、鍵の存在チェックだけでなく毎回SHA-256照合してから表示**（`verifyNsfAdmin`）。
  - 表示条件は3つ: 照合OK ＋ `lastRecordingBlob` あり ＋ `currentWork.id` あり。`finishRecordingBlob` / `resetRecordingOutput` / `applyWorkInfo`（currentWork代入）の3箇所で `updateShareToSnsUi()` を呼ぶ。
- キャプション: `buildSnsVideoCaption` — **works.html の `buildWorkCaption` と同一書式**（shortDesc＋longDesc＋作品番号＋OGP付き作品ページURL）。書式を変えるときは両方揃えること。
- 検証: 一般利用者=非表示／誤った鍵をsessionStorageに直接入れても照合で拒否／3条件成立でのみ出現／録画消去で非表示復帰／キャプション書式一致、の6項目をブラウザ実測。`check_public_leak.py` 高リスク0件（index.htmlの「.env参照×2」は `ort.env`（ONNX Runtime API）の誤検知）。本番反映grep 11箇所。
- 注意:
  - ⛔ このボタンにAPIトークンを持ち込まないこと。自動投稿化したくなったら既存のThreads GAS（サーバー側にトークン保持）経由で設計する。
  - JSゲートはUIの出し分けであって機密保護ではない（静的ホスティングの教訓）。このボタンが扱うデータはすべて公開済み（作品集カタログ・自分で生成した動画）なので成立している設計。非公開データを足さないこと。

## 2026-08-30 — SNS動画メーカー: 通常モードの「締めのロゴ」CTA（ON/OFF付き）

- 対象: `digital-room/sns-video-maker/index.html`
- 要望: フィルムシーンだけでなく通常モードでも、最後にロゴを浮かび上がらせたい（事業主）。
- 実装: **オーバーレイ方式**。`endLogoCta` チェックボックス（既定OFF）ONで、動画の最後の約2.6秒（`END_LOGO_CTA_SEC`）に暗幕 rgba(10,8,6,.62) ＋ 金のフクロウ・ワードマーク（`brandAssets.owlGold/wordmarkGold` を新規ロード）がフェードイン＋2%上昇。
  - **総尺・セリフ音声の同期に一切触らない**（最後のスライドの上に重ねるだけ）。エンドロール実装を保留した理由だった改修リスクを回避した設計。
  - `drawFrame` の通常経路末尾で呼ぶため、プレビュー・全保存モードに自動で効く。
  - **フィルムシーン使用中は発動しない**（CTAコマが担うため。二重ロゴ防止）。
  - ロゴ2枚とも未読込のときは文字 "N's factory" でフォールバック（真っ暗な締めを防ぐ）。
- 検証: OFF時は終盤も金色0px／ON時は終盤のみ金色出現（中盤0px）／フェードの立ち上がり（0→303px）／フィルムモード中はON/OFFで描画不変、を実測。金ロゴPNGは検証環境（data:URL）では読めず**文字フォールバックの発動を実測**。本番は既存白ロゴと同じ assets パスで、直リンクHTTP 200と配信HTMLへの反映（grep 9箇所）を確認。
- 注意:
  - CTAの尺を変えるときは `END_LOGO_CTA_SEC` 1箇所。
  - 「セリフが終わるまで写真を待たせる」で総尺が動いても、`totalDuration()` を都度参照しているため常に「最後の2.6秒」に張り付く。

## 2026-08-26 — SNS動画メーカー: 原稿長からの速度逆算（自動調整）＋締めのCTAコマ

- 対象: `digital-room/sns-video-maker/index.html`
- ① `filmSpeedAuto` チェックボックス「原稿の長さに合わせて自動調整」:
  - `secPerFrame = max(totalDuration() / frames.length, .5)` を**描画のたびに計算**。totalDuration はセリフ同期で延びた表示秒数の合計＝原稿駆動の総尺なので、AIナレーション保存時（実音声で秒数確定後）も自動で「最後のセリフ終了＝ちょうど1周」になる。
  - ON中は `filmSpeed` 数値入力を disabled。OFFで手動値に戻る。
- ② CTAコマ `filmCtaFrame(frameW, frameH)`:
  - コマ列の最後に自動連結（`frames = 写真 + [CTA]`）。**自動速度の分母もCTA込みのコマ数**。
  - 焦げ茶地 #171310＋金のグロー、フクロウロゴ（owlWhite）上寄せ、「一冊ずつ、手で縫っています。」、下端にワードマーク（wordmarkWhite、未ロード時はテキストで代替）。
  - キャッシュはサイズ＋ロゴ読み込み状態がキー。ロゴが後から読み込まれたら作り直される。
- 検証（ブラウザ実測）: CTAコマ単体生成（明色=文字/ロゴ・暗色=地）／キャッシュ再利用／**t=0とt=総尺で差分0画素（CTA込み4コマでちょうど1周）**／終盤（総尺7/8時点）に中央帯へCTAのクリーム色23,942px出現。構文チェック・本番反映grep確認済み。
- 注意:
  - CTA文面を変えるときは `filmCtaFrame` 内の文字列1箇所。売り込みコピー（今すぐ購入等）は入れない（SNS発信方針 第6章）。
  - **前回検証の教訓**: 「1周で完全一致」の検証はセリフ・キャプションを空にして行う（字幕はタイムライン追従のため一致しなくて正しい）。ハッシュのサンプリング判定より `getImageData` 全画素差分の方が誤判定がない。

## 2026-08-26 — SNS動画メーカー: フィルム配色をダークブラウン透過へ・端部コントラスト改善

- 対象: `digital-room/sns-video-maker/index.html`（drawFilmStripScene のみ）
- 事業主指摘「フィルムの端部が背景の黒と重なって見づらい」→ 続けて「フィルムはダークブラウンの透過色がよい」。
- 最終配色:
  - 背景: 中央 #2A2622 → #171512 → 隅 #0A0908 のラジアルグラデーション（平坦な黒だと帯が同化するため）
  - フィルムベース: `rgba(54,37,22,.86)`（ダークブラウン透過。実物のネガのベース色）
  - 上下縁ハイライト: `rgba(255,214,150,.32)` 4px（アンバー系。ブランドのシャンパンゴールドに馴染む）
  - パーフォレーション穴: 従来のクリーム色のまま（ライトテーブルの光の見立て）
- 検証（ブラウザ実測・法線方向の画素走査）: ベース帯 rgb(51,36,23)＝R>G>B の茶系／縁ライン輝度97 vs 背景輝度29（輝度差68）で輪郭明瞭／上下の縁とも同値。本番反映は配信HTMLを色コード3種でgrepして確認（デプロイrunの時刻ではなく実物で判定）。
- 注意: 配色を変えるときは「背景グラデ・ベース・縁・穴」の4点セットで考えること。1点だけ変えると再び同化や浮きが起きる。

## 2026-08-26 — SNS動画メーカー: フィルムの速さは数値直接入力が最終形（セレクトは同日廃止）

- 対象: `digital-room/sns-video-maker/index.html`
- 経緯: 速度セレクト4択（3.5/2.2/1.5/1.0秒）を実装 → 事業主「全て早すぎる」→ 遅い側4択（10/7/5/3.5秒）へシフト → 「もっと幅を」「自分で数値を入れたい」「UIをごちゃつかせない」→ **数値入力1欄が最終形**。
- 最終仕様: `filmSpeed` は `input type=number`（min1 / max60 / step0.5 / 既定8秒=1コマの秒数）。フィルムシーンON時のみ表示。数字が大きいほどゆっくり。
  - 描画側は `clamp(parseFloat(...) || 8, 1, 60)`。手入力の範囲外・空欄・文字でも壊れない。
  - `input` イベントで即時再描画（ログなし）、`change` で resetRecordingOutput＋ログ。キーストロークごとにログを連発しない。
  - 「動きの強さ」との連動は廃止のまま（速さはこの欄一本）。
- 検証（ブラウザ実測7項目）: 型・既定・範囲／ON時のみ表示／任意値8・13.5・40秒で描画位置が全て変化／999→60扱い・0.1→1扱いのクランプ／空欄→8秒フォールバック／inputイベント即反映。構文チェック・本番反映確認済み。
- 注意: 既定8秒は事業主の体感（3.5秒でも速すぎる）に基づく。今後速度の既定を変えるときはHTMLのvalueとJSフォールバックの**2箇所**を揃えること。

## 2026-08-25 — SNS動画メーカー: フィルムシーン（全編）モードを追加

- 対象: `digital-room/sns-video-maker/index.html`
- 要望: 「ネガフィルムみたいにすべての画像がつながって斜めに流れていくシーン」（事業主）。配置の3択に対し「全てを繰り返しリピート」の回答だったため、**動画全編をフィルムストリップにし写真を循環リピート**させるモードとして実装。色味は「写真のまま」（革の色味を変えない方針）。
- 実装:
  - 出力設定「動きの強さ」の下に `filmStripMode` セレクト（使わない／全写真をネガフィルム風に斜めへ流す）。既定OFF。
  - `drawFrame` の先頭で `filmStripActive()` なら `drawFilmStripScene(time, activeSlide, progress)` へ分岐。**frameState のタイミング計算はそのまま**なので、セリフ音声・字幕・台本表示・保存の全経路が無改修で追従する。
  - 描画: 暗背景 → 回転座標系（-18度）にフィルムベース帯 → コマ（横3:2、循環 `((k%n)+n)%n`、cover-crop）→ 上下パーフォレーション（穴はストリップと同速で移動）→ ビネット → `drawTexts` / `drawNarrationSubtitle`（いま読んでいる写真基準）。
  - 速さ: 標準で1コマ約2.2秒。`motionIntensity().scale` (1/4/6/7) を係数 1/1.6/2.1/2.7 に対応付け。
  - 変更リスナーは motionIntensity と同作法（resetRecordingOutput + 再描画 + setLog）。
- 検証（ブラウザ実測・ダミー写真3枚）: 対角線サンプリングで隣接コマの写真色が連続（緑→青）／クリーム色ピクセル25,071個＝穴が描画／全画面ハッシュで t=0.5 と 1.2 が異なる（動いている）／**1周期（枚数×2.2秒）後のフレームが完全一致（ループ切れ目なし）**／同じtは同じ絵（MP4エンコードの決定論性）／OFFで通常描画へ復帰。構文チェック通過・本番反映確認。
- 注意:
  - ⛔ このモード中は写真ごとのカメラワーク・切替効果は**視覚上無効**（時間割だけ使う）。仕様であり、ヘルプ文にも記載済み。
  - コマ順は playableSlides() の並び順。並べ替えはそのまま効く。
  - `drawFilmStripScene` は `roundRect`（既存ヘルパー・パス生成型）に依存。ヘルパーの挙動を変えるときは穴の描画を確認すること。

## 2026-08-22 — 動画メーカー: Drive読み込み中の進行バナーを追加

- 対象: `digital-room/sns-video-maker/index.html`（GASは無改修）
- 症状: register.html の「動画をつくる」から遷移すると、写真が並ぶまで何も起きていないように見え、読み込み中か固まったのか分からない。
- 原因: 写真が出るまでに **5段階**（①作品情報の取得 ②Drive写真の取得 ③画像処理 ④作品情報の反映 ⑤長文英訳）を通るが、**①③④は完全に無表示**、②⑤も静的な1行だけ。さらに `setDriveStatus()` の出力先 `.drive-status` は `<details class="drive">` の中にあり、**閉じていると見えない**。
- 対策:
  - 折りたたみの外・写真パネル最上部に `#driveProgress` を新設。スピナー＋段階名＋**経過秒**＋「ステップ n / 5」。
  - `setDriveProgress(step, label)` / `stopDriveProgress()` を追加。1秒ごとに経過秒を書き換えるので、**数字が増え続けていれば生きている**と利用者が判別できる（2026-08-20 の progressTicker と同じ思想）。
  - URL起動時は最初の `syncWorkFromNumber()` から表示し `try/finally` で必ず消す。候補なしの早期return経路にも `stopDriveProgress()` を置いた。
- 実測（本番）: 読み込み全体で **28秒**（うちステップ2のDrive写真取得が約14秒以上）。別の回では **68秒**でもカウンターは正常。モバイル375pxで幅317・横スクロールなし・JSエラー0。
- 注意:
  - ⛔ `setDriveProgress` から `setLog` / `els.log` を触らないこと。毎秒呼ぶとスマホのトーストが出っぱなしになる（progressTicker と同じ罠）。
  - このバナーは**速度改善ではない**。Drive写真取得API自体の軽量化は未着手。
  - 段階を増やすときは `DRIVE_PROGRESS_STEPS` の配列も更新すること（「ステップ n / 5」の分母が配列長）。

## 2026-08-20 — SNS動画メーカー: 処理中の経過秒カウンター（動作中か固まったかの判別）

- 対象: `digital-room/sns-video-maker/index.html`
- 症状: 「7/8枚目のAIナレーションを生成中です。」が静止表示のため、**処理が進んでいるのか固まっているのか利用者が判別できない**（事業主報告）。実際は動作中だった。
- 対策: `startProgressTicker` / `updateProgressTicker` / `stopProgressTicker` を追加。1秒ごとに `（n秒経過）` を書き換える。
  - **数字が増え続ける＝生きている / 止まる＝固まっている**、を利用者自身が判別できる設計。
  - 稼働段階: AI音声生成（枚数ごとに見出し差し替え）→ AAC変換 → MP4合成。**経過秒は通しで継続**し、映像合成に入るときだけ `{resetElapsed:true}` でリセット。
  - AI音声生成中は `progressBar` も進める（従来は映像エンコード時のみ）。
  - `waitForEncoderQueue` の5秒通知も ticker へ統合。
  - 停止は `recordVideo` の `finally`（成功・失敗・中止の全経路が通る）。
- 注意:
  - ⛔ ticker 稼働中に `setLog` を呼んでも**毎秒の書き換えで即上書きされて見えない**。進行中の文言変更は必ず `updateProgressTicker` を使うこと。
  - ticker の2回目以降は `els.log.textContent` を直接書く。`setLog` を毎秒呼ぶとスマホのトーストが出っぱなしになるため。
  - 止め忘れると数字が動き続けて「まだ処理中」と誤解させる。出口を増やすときは必ず `stopProgressTicker()` を通すこと。
- 検証: 構文チェック通過／ブラウザ実測で 0→2→4秒のカウントアップ・段階変更時の見出し差し替えと経過秒継続・`{resetElapsed:true}` でのリセット・停止後に増えないこと・タイマー残留なしを確認。
  - ⚠ 検証時に秒数が実時間の2倍に見えたが、切り分けの結果**バックグラウンドタブのタイマー間引き**（`setTimeout(1000)` の実所要が1975ms、`performance.now()` と実時計は一致）であり、カウンター側の計算は正確だった。**バックグラウンドタブでは表示更新が飛び飛びになる**点は仕様として許容。

---

## 領域別ノート

> 直近の記録のうち、サイト本体と別領域のものはここに分けてある。**その領域を触るときだけ開く。**

### [監理幕僚室ツール](docs/notes/kanri-room.md) — 10件
`order_estimate/admin.html`（証憑チェックリスト）/ `kanri-room/`（レシート取込・暗号資産）/ `invoice/`（請求書）

- 2026-08-24 — レシート取込: 突合の誤マッチ防止（店名なし照合の廃止・「要確認」の新設）
- 2026-08-24 — 書類番号を「保存時に採番」へ変更（開くだけで番号が飛ぶ問題）
- 2026-08-24 — 請求書の宛先住所を任意表示に
- 2026-08-23 — 証憑チェックリスト: ラクマ列の追加と、会計送信の二重計上防止
- 2026-08-23 — 監理幕僚室に「レシート取込（スマホ）」を新設
- 2026-08-23 — レシート取込: 経理ルールを非公開化＋証憑チェックリストにスマホ取込列
- 2026-08-23 — レシート取込: 科目が「不明」しか選べない障害の修正＋科目ボタン化
- 2026-08-22 — 証憑チェックリスト: 証憑の中身をクリックで確認できるように
- 2026-08-22 — 証憑チェックリスト: 取得が止まっていることを画面で分かるように
- 2026-08-21 — 監理幕僚室に暗号資産 計算書ツールを新設

### [ダイエット管理アプリ（PWA）](docs/notes/diet-app.md) — 2件
`diet/`

- 2026-09-04 — 写真解析の「GAS承認待ち」エラーを解消（間欠404の自動再送）
- 2026-08-24 — ダイエット管理アプリ（PWA）を新設 `/diet/`

---

## 過去の作業記録（アーカイブ）

> **通常セッションでは開かない。** 該当箇所を触るときだけ読む。
> 恒久的な禁止事項は上の「⛔ 恒久ルール」に索引がある（出典に「アーカイブ」と付いているものがここ）。

### [2026年8月](docs/worklog/2026-08.md) — 26件
8月19日まで。撤収済みのThreads言及監視3件を含む。

- 2026-08-25 — Threads言及監視（ブランドモニタリング）を新設 ※撤収済み
- 2026-08-25（続報）— Threads言及監視: 起票先をprivate化＋App Review準備 ※撤収済み
- 2026-08-25（撤収）— Threads言及監視を取りやめ・関連ファイルを削除
- 2026-08-12 — SNS動画メーカー: 保存失敗の「嘘の案内」修正＋診断情報＋成否統計
- 2026-08-12 — SNS動画メーカー: 作品集を静的JSON優先に切替（GAS断続障害の構造的回避）
- 2026-08-12 — SNS動画メーカー: 「写真を全部クリア」ボタンを追加
- 2026-08-12 — SNS動画メーカー: Drive通信の自動リトライ＋誤解を招くエラー文言の修正
- 2026-08-12 — 登録・更新完了時に作品集静的データを即時再生成
- 2026-08-12 — 作品を名指しで案内するリンクとSNSプレビュー
- 2026-08-12 — Threadsでプレビューが出ない件の修正（OGPの落とし穴2つ）
- 2026-08-12 — ?id= で詳細が開かない不具合の修正
- 2026-08-06 — ⚠️ 前項の前提が誤りだった／お知らせ欄を「足し込み」描画に修正
- 2026-08-06 — お知らせのThreads告知コードを確定（GAS側・docs のみ）
- 2026-08-06 — NewsPublish.gs 全容判明／NEWS_LINE 形式を追加
- 2026-08-06 — 過去分の「読み込み」がtimeoutになる件（読み取り系にリトライを追加）
- 2026-08-06 — お知らせ承認を「メール返信」方式に変更（スマホ対応）
- 2026-08-06 — 候補メールの文面を返信方式に合わせる
- 2026-08-06 — 作品集カテゴリを3段階（大→中→小）に再編
- 2026-08-06 — サイズ仕分けページ新設＋作品詳細→登録画面の直行ボタン
- 2026-08-06 — 手帳カバーのサイズを判型系に分離＋仕分けページに自由入力
- 2026-08-05 — お知らせ欄「掲載する」ボタンが効かない件（原因調査＋手動掲載）
- 2026-08-05 — 作品集にテクスチャシミュレーター導線を追加
- 2026-08-05 — 更新時の動画導線追加＋テクスチャ判定の軽量API化
- 2026-08-05 — お知らせ欄をデータ駆動化し「掲載する」の受け口を作成
- 2026-08-04 — 商品登録の二重登録バグ修正（Google間欠障害への冪等リトライ）
- 2026-08-04 — 作品集 SOLDOUTのホワイトアウト廃止

### [2026年7月](docs/worklog/2026-07.md) — 2件

- 2026-07-15 — 商品登録画面「登録する」ボタン無効化の修正
- 2026-07-15 — 商品登録画面の下書き自動保存・復元
